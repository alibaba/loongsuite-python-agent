---
description: 如何为 Python 框架找到合适的 instrumentation 点位（埋点位置）
---

# 找点位 (Locate Instrumentation) 指南

## 什么是"找点位"

"找点位"是为 Python 框架编写 instrumentation（埋点/插桩）之前的关键步骤。目标是在目标框架的源码中找到合适的方法，用于后续通过 Python 装饰器或 wrapt wrapper 进行 patch。

**相关文档**: [框架类型与追踪操作定义](mdc:.cursor/rules/framework-types-and-operations.mdc) - 了解不同类型框架需要追踪的关键操作

## 完整工作流程概览

"找点位"任务的完整工作流程包含以下关键步骤：

```
1. 搜索开源社区实现 (使用 DeepWiki)
   ↓
2. 有找到开源实现？
   ├─ 是 → 分析并写入调研报告到 memory/
   └─ 否 → 继续下一步
   ↓
3. 理解框架核心功能
   ↓
4. 分析框架源码结构 (参考开源实现)
   ↓
5. 评估候选点位 (对比开源实现)
   ↓
6. 验证可行性
   ↓
7. 输出结果
   ├─ 控制台：点位列表 + 理由说明 + 开源对比
   └─ Memory：写入点位选择报告到 memory/
```

### 关键输出物

1. **开源调研报告** (如有)：`memory/instrumentation-research-[框架名称]-[日期].md`
2. **点位选择报告**：`memory/instrumentation-locations-[框架名称]-[日期].md`
3. **控制台输出**：包含点位列表、理由说明、风险提示和开源对比

## 核心概念

- **Instrumentation（埋点/插桩）**: 通过 Python 修饰器或 wrapper 对框架方法进行 patch，在方法执行前后注入额外行为（如记录 trace、metrics）
- **点位**: 被选中用于 patch 的目标方法
- **Patch 规则**: 在 instrumentation 项目的 `__init__.py` 的 `_instrument` 方法中声明
- **Wrapper 实现**: 在 instrumentation 项目的 `patch.py` 中实现具体的埋点逻辑

## 好的点位必须满足的标准

### 1. 框架代码的一部分
- ✅ 点位必须是目标框架内部定义的方法
- ❌ 不应该是应用代码或第三方依赖的方法

### 2. 关键调用路径上的核心方法
- ✅ 主要业务逻辑在此方法中完成
- ✅ 方法的执行耗时最大程度代表整个关键调用的耗时
- 示例：对于 HTTP 调用，点位应该覆盖"发送请求 + 接收响应"的完整过程

### 3. 携带完整的调用信息
- ✅ 方法的参数和返回值包含需要记录的完整信息
- 示例：HTTP 调用需要包含 method、path、params、headers 等
- 示例：LLM 调用需要包含 model、messages、temperature 等配置

## 好的点位建议满足的标准（如有特殊情况，可以考虑不满足，并说明原因）

### 1. 公开的 API
- ✅ 框架对外暴露的公共 API
- ✅ 更容易支持多个版本
- ✅ 不容易因内部重构而失效
- 避免 patch 私有方法（以 `_` 开头）或内部实现细节

### 2. 普适性
- ✅ 一个点位能覆盖尽可能多的使用场景
- ✅ 不要为每个特殊场景都创建单独的点位
- 优先选择通用的入口方法

## 找点位的方法论

### 步骤 1: 搜索开源社区已有实现

在开始分析框架源码之前，先搜索开源社区是否已有类似的 instrumentation 实现。

**操作步骤**:
1. 使用 DeepWiki MCP 工具搜索三个推荐的开源项目：
   - Arize OpenInference (`Arize-ai/openinference`)
   - Traceloop OpenLLMetry (`traceloop/openllmetry`)
   - Langfuse Python SDK (`langfuse/langfuse-python`)

2. 对于每个项目，使用 `mcp_deepwiki_ask_question` 询问：
   - "Does this project have instrumentation for [目标框架名称]?"
   - "Where are the instrumentation locations for [目标框架名称]?"

3. **如果找到已有实现**：
   - 分析各个项目的点位选择
   - 理解为什么选择这些点位
   - 对比不同项目的实现差异
   - 将调研结果总结并写入 `memory/` 目录
   - 文件命名：`memory/instrumentation-research-[框架名称]-[日期].md`

4. **如果没有找到已有实现**：
   - 记录搜索结果（未找到）
   - 继续执行后续步骤

### 步骤 2: 理解框架的核心功能
- 识别框架类型（HTTP Server/Client、RPC、DB、MQ、LLM 等）
- 明确要追踪的关键操作（参考 [框架类型与追踪操作定义](mdc:.cursor/rules/framework-types-and-operations.mdc)）
- 从用户使用的角度识别主要的 API 入口

### 步骤 3: 分析框架源码结构
- 定位核心模块和类
- 识别公共 API 的入口点
- 理解调用链路和数据流
- **如果有开源参考**：对比参考实现的点位选择

### 步骤 4: 评估候选点位
- 对每个候选点位应用上述标准（必须满足的 + 建议满足的）
- 检查方法签名：参数和返回值是否包含所需信息
- 验证执行路径：是否覆盖完整的操作生命周期
- **如果有开源参考**：验证参考实现是否满足标准

### 步骤 5: 验证可行性
- 检查是否支持同步和异步两种调用方式
- 确认是否需要处理流式返回（streaming）
- 评估版本兼容性
- **如果有开源参考**：了解参考实现如何处理这些情况

## 项目中的优秀示例

### OpenAI SDK Instrumentation
参考 [opentelemetry-instrumentation-openai-v2](__init__.py:instrumentation-genai/opentelemetry-instrumentation-openai-v2/src/opentelemetry/instrumentation/openai_v2/__init__.py)

**找到的点位**:
```python
# Chat completions
openai.resources.chat.completions.Completions.create
openai.resources.chat.completions.AsyncCompletions.create

# Embeddings
openai.resources.embeddings.Embeddings.create
openai.resources.embeddings.AsyncEmbeddings.create
```

**为什么这些是好点位**:

*必须满足的标准*:
1. ✅ **框架代码的一部分**: 是 OpenAI SDK 的核心 API
2. ✅ **关键调用路径**: `create` 方法执行完整的 API 调用（发送请求 + 接收响应）
3. ✅ **完整信息**: 参数包含所有配置（model, messages, temperature 等），返回值包含完整响应

*建议满足的标准*:
4. ✅ **公开 API**: 公开的、文档化的 API，版本稳定
5. ✅ **普适性**: 同时支持同步/异步、流式/非流式，覆盖所有场景

查看实现细节：
- Patch 规则声明：[__init__.py](__init__.py:instrumentation-genai/opentelemetry-instrumentation-openai-v2/src/opentelemetry/instrumentation/openai_v2/__init__.py)
- Wrapper 实现：[patch.py](patch.py:instrumentation-genai/opentelemetry-instrumentation-openai-v2/src/opentelemetry/instrumentation/openai_v2/patch.py)

## 开源社区的参考实现

在"找点位"过程中，也可以参考开源社区中已有的优秀 instrumentation 实现，使用 deepwiki MCP 工具学习其他项目是如何为各种框架选择埋点位置的。

### 推荐的开源项目

以下三个开源项目提供了丰富的 instrumentation 实现，特别是针对 LLM 和 AI 框架：

#### 1. Arize OpenInference
- **项目地址**: https://github.com/Arize-ai/openinference
- **特点**: 
  - 专注于 AI/ML 可观测性
  - 提供了大量 LLM 框架和向量数据库的 instrumentation
  - 包含 LangChain、LlamaIndex、OpenAI、Anthropic 等的实现
  - 遵循 OpenTelemetry 语义规范
- **适合参考的场景**: LLM Client、LLM 框架、RAG、向量数据库

#### 2. Traceloop OpenLLMetry
- **项目地址**: https://github.com/traceloop/openllmetry
- **特点**:
  - 专门为 LLM 应用设计的可观测性工具
  - 支持多种 LLM 提供商（OpenAI、Anthropic、Cohere、Bedrock 等）
  - 涵盖主流 LLM 框架（LangChain、LlamaIndex、Haystack 等）
  - 提供了丰富的向量数据库 instrumentation
- **适合参考的场景**: LLM Client、LLM 框架、向量数据库、Agent 框架

#### 3. Langfuse Python SDK
- **项目地址**: https://github.com/langfuse/langfuse-python
- **特点**:
  - LLM 工程平台的 Python SDK
  - 提供了 LangChain、OpenAI、LlamaIndex 等的集成
  - 关注 LLM 应用的 tracing 和评估
  - 包含详细的 prompt 和 token 追踪
- **适合参考的场景**: LLM 框架集成、Prompt 追踪、Token 统计

### 使用 DeepWiki 工具的完整工作流程

当为某个框架寻找点位时，按以下流程使用 DeepWiki 工具：

#### 第一步：搜索已有实现

对每个推荐的开源项目，使用 `mcp_deepwiki_ask_question` 工具询问：
- "Does this project have instrumentation for [框架名称]?"

**记录结果**：哪些项目有实现，哪些没有。

#### 第二步：分析点位选择

对找到实现的项目，继续询问：
- "Which methods have been patched by the instrumentation for [框架名称]?"
- "What classes and methods are instrumented for [框架名称]?"

**收集信息**：
- 具体的模块、类、方法路径
- 为什么选择这些点位
- 选择的理由和依据

#### 第三步：学习实现细节

深入了解实现方式：
- "How does [项目名] handle async/sync for [框架名称] instrumentation?"
- "How does [项目名] handle streaming responses for [框架名称]?"
- "What attributes are captured in [框架名称] instrumentation?"

**关注要点**：
- 同步/异步处理方式
- 流式响应处理方式
- 提取的关键信息
- 异常处理策略

#### 第四步：对比多个实现

如果多个项目都有实现，进行对比分析：
- 找出共同的点位选择（这些通常是关键点位）
- 识别实现差异的原因
- 评估不同方案的优劣

#### 第五步：写入调研报告

将调研结果写入 `memory/instrumentation-research-[框架名称]-[日期].md`，内容包括：
- 搜索的项目列表
- 找到的实现详情
- 点位选择对比
- 实现要点总结
- 推荐的参考方案

#### 示例：分析 LangChain 的 instrumentation

```
问题 1: "Arize-ai/openinference - How does the LangChain instrumentation work?"
- 了解 OpenInference 的 LangChain 实现

问题 2: "traceloop/openllmetry - Where are the instrumentation locations for LangChain?"
- 了解 OpenLLMetry 的 LangChain 点位选择

问题 3: "langfuse/langfuse-python - How to integrate with LangChain?"
- 了解 Langfuse 的集成方式

通过对比三个项目的实现，可以：
- 找到共同的点位选择（说明这些是关键点位）
- 发现不同的实现策略
- 学习最佳实践
```

### 参考时的注意事项

1. **版本差异**: 开源项目可能支持的框架版本与你的目标版本不同
2. **语义规范**: 不同项目可能遵循不同的语义规范或自定义规范
3. **实现复杂度**: 根据项目需求调整，不必完全照搬复杂的实现
4. **License 兼容性**: 注意开源项目的 License，确保兼容
5. **代码质量**: 批判性地参考，不是所有实现都是最优的

### 参考优先级

在找点位时，建议按以下优先级参考：

1. **首选**: 本项目中已有的类似 instrumentation 实现
2. **次选**: 上述三个开源项目的实现
3. **辅助**: 框架官方文档和源码分析
4. **兜底**: 社区讨论和 issue

通过结合项目内实现、开源社区实践和框架源码分析，可以更准确、更快速地找到合适的点位。

## 执行"找点位"任务时的注意事项

### 关于开源调研

1. **优先搜索开源实现**: 在分析框架源码之前，先使用 DeepWiki 搜索三个推荐的开源项目
2. **批判性参考**: 开源实现不一定是最优方案，需要结合实际需求判断
3. **记录调研过程**: 无论是否找到开源实现，都要记录搜索过程和结果
4. **写入 memory**: 如果找到开源实现，必须将调研结果写入 `memory/instrumentation-research-[框架名称]-[日期].md`

### 关于框架分析

5. **需要框架源码**: 使用者必须提供目标框架的源码以便进行代码分析
6. **多个候选点位**: 可能有多个可行的点位，需要权衡选择最优方案
7. **同步 vs 异步**: 现代框架通常同时提供同步和异步 API，都需要考虑
8. **流式响应**: 某些操作（如 LLM streaming）需要特殊处理
9. **版本差异**: 分析时要考虑框架的主要版本差异

### 关于输出结果

10. **双重输出**: 必须同时输出控制台展示和 memory 文件
11. **Memory 文件命名**: 严格按照规定格式命名，包含框架名称和日期
12. **结构化输出**: memory 文件必须遵循规定的 Markdown 结构
13. **包含开源对比**: 如果有开源参考，在输出中必须包含对比分析
14. **后续跟踪**: memory 文件中要包含"下一步行动"清单，便于后续实现

## 输出格式

找点位任务完成后，应输出以下内容：

### 1. 控制台输出

向用户展示的主要内容：

#### 1.1 开源实现调研结果（如有）
- **搜索的项目**: 列出查询了哪些开源项目
- **找到的实现**: 简要说明找到了哪些项目有类似实现
- **关键发现**: 
  - 共同的点位选择
  - 不同的实现策略
  - 值得借鉴的做法
- **参考链接**: 指向 `memory/instrumentation-research-[框架名称]-[日期].md` 的详细调研报告

#### 1.2 推荐的点位列表
- **模块路径**: 完整的 Python 模块路径
- **类名**: 目标类的名称
- **方法名**: 需要 patch 的方法名
- **是否异步**: 标注是否需要同时处理同步和异步版本

#### 1.3 理由说明
- **必须满足的标准**: 
  - 为什么是框架代码的一部分
  - 为什么在关键调用路径上
  - 携带了哪些完整信息
- **建议满足的标准**:
  - 是否是公开 API（如不是，说明原因）
  - 是否具有普适性（如不是，说明原因）
- **特殊考虑**:
  - 如何处理流式响应
  - 如何处理异步调用
  - 版本兼容性说明

#### 1.4 潜在风险
- 版本兼容性问题
- 可能的限制或缺失信息
- 需要特殊处理的场景

#### 1.5 与开源实现的对比（如有）
- 本方案与开源实现的异同
- 选择差异的原因
- 本方案的优势

### 2. Memory 目录输出

将找点位的结果写入 `memory/` 目录，便于后续参考和迭代。

#### 2.1 开源调研报告（如有找到开源实现）
**文件路径**: `memory/instrumentation-research-[框架名称]-[日期].md`

**内容结构**:
```markdown
# [框架名称] Instrumentation 开源实现调研

## 调研时间
YYYY-MM-DD

## 调研的开源项目
1. Arize OpenInference
2. Traceloop OpenLLMetry  
3. Langfuse Python SDK

## 找到的实现

### 项目1: [项目名称]
- **仓库地址**: 
- **点位选择**: 
- **实现要点**: 
- **特殊处理**: 

### 项目2: [项目名称]
...

## 对比分析
- **共同点位**: 
- **实现差异**: 
- **最佳实践**: 

## 参考建议
- 推荐采用的点位
- 推荐采用的实现方式
- 需要注意的问题
```

#### 2.2 点位选择报告
**文件路径**: `memory/instrumentation-locations-[框架名称]-[日期].md`

**内容结构**:
```markdown
# [框架名称] Instrumentation 点位选择

## 基本信息
- **框架名称**: 
- **框架类型**: (HTTP Server/Client, RPC, DB, MQ, LLM Client, LLM Framework 等)
- **框架版本**: 
- **分析日期**: YYYY-MM-DD
- **是否有开源参考**: 是/否

## 推荐的点位

### 点位 1: [方法全路径]
- **模块**: 
- **类**: 
- **方法**: 
- **是否异步**: 
- **满足标准**:
  - 必须标准: ✅/❌
  - 建议标准: ✅/❌
- **捕获信息**: 
- **特殊处理**: 

### 点位 2: [方法全路径]
...

## 理由说明

### 为什么选择这些点位
...

### 与开源实现的对比（如有）
...

## 实现注意事项
- 同步/异步处理
- 流式响应处理
- 版本兼容性
- 异常处理

## 潜在风险
...

## 下一步行动
- [ ] 实现 instrumentation 代码
- [ ] 编写测试用例
- [ ] 验证版本兼容性
```

### 3. 输出说明

- **一般情况**: 不需要输出示例代码，除非使用者主动要求
- **复杂情况**: 如果点位选择涉及复杂的判断逻辑或特殊处理，可以提供关键代码片段说明
- **Memory 文件**: 所有 memory 文件使用 Markdown 格式，便于阅读和版本管理
